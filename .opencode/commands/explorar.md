---
description: Explora código y documentación en modo lectura usando el subagente explorador.
agent: explorador
---

Explora el repositorio de AgroGest VSM en modo solo lectura.

Consulta del usuario:

```text
$ARGUMENTS
```

Reglas:

- Lee `AGENTS.md` y `docs/index.md` si el tema es amplio.
- Usa solo comandos de inspección permitidos.
- No edites archivos.
- Distingue hechos observados de inferencias.
- Devuelve rutas concretas y riesgos relevantes.
